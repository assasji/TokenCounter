# Security Policy

## Reporting a vulnerability

보안 문제는 공개 GitHub 이슈로 먼저 올리지 말고 저장소의 **Security → Report a vulnerability**를 통해 비공개로 신고해 주세요.

신고에는 영향을 받는 버전, 재현 단계, 예상 영향만 포함해 주세요. 실제 access token, refresh token, Keychain 내보내기, `auth.json` 원문은 첨부하지 마세요.

## Supported versions

공개 릴리스 전에는 기본 브랜치의 최신 코드만 지원합니다. 릴리스를 시작한 뒤에는 이 문서에 보안 업데이트를 받을 버전 범위를 명시해야 합니다.

## Credential handling

TokenCounter는 공급자별 로그인 정보로 각 공급자에게 직접 요청합니다. 앱은 자격 증명을 자체 서버로 전송하거나 로그에 기록해서는 안 됩니다. 이 원칙을 위반할 가능성이 있는 문제는 보안 취약점으로 신고해 주세요.

Codex는 앱의 브라우저 OAuth 로그인만 사용하며 CLI 인증 파일·Keychain에는 접근하지 않습니다. 요청된 설계에 따라 access/refresh 토큰을 UserDefaults에 저장합니다. **이는 암호화 저장소가 아니며, 앱 환경설정 백업도 비밀정보로 취급해야 합니다.** 같은 사용자 권한의 악성 프로그램으로부터 토큰을 보호하지 못합니다. 로그아웃은 앱의 로컬 저장값만 삭제하며 서버 토큰 철회·백업 삭제를 보장하지 않습니다.

Codex 콜백은 loopback에만 바인딩하고 정확한 호스트·포트·경로와 일회성 state를 확인합니다. PKCE S256, 로그인 제한 시간, 단일 코드 교환, 토큰 갱신 중복 방지 및 로그아웃 후 응답 무효화를 적용합니다. 인증은 앱 안의 작은 WebKit 창에서 진행하며 외부 브라우저나 그 쿠키·비밀번호·프로필 DB를 읽거나 복사하지 않습니다. 공급자 자체의 세션 만료·동의·추가 인증 요구를 우회하지 않습니다.

타 공급자의 인증 코드와 폴백은 독립적입니다. Codex 변경을 전체 앱의 보안 검토 완료 또는 공급자의 OAuth 클라이언트 사용·재배포 승인으로 해석해서는 안 됩니다.
